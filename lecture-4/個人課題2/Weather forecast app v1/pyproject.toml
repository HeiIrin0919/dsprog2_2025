[project]
name = "weather-forecast"
version = "1.0.0"
description = "気象庁APIを利用した天気予報アプリケーション"
requires-python = ">=3.8"
dependencies = [
    "flet>=0.21.0",
    "requests>=2.28.0",
]

[project.scripts]
weather = "weather:main"
